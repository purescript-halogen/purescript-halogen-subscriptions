{
  "private": true,
  "scripts": {
    "build": "spago build --purs-args '--censor-lib --strict'",
    "test": "spago -x test/test.dhall test"
  },
  "devDependencies": {
    "purescript": "^0.15.0-0",
    "purescript-psa": "^0.8.2",
    "spago": "^0.20.8"
  }
}
